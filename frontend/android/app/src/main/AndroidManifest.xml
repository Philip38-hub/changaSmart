<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- Read-only access to the SMS inbox that already exists on the
         device, so the M-PESA Inbox screen can browse existing messages
         (see frontend/lib/screens/mpesa_inbox/). -->
    <uses-permission android:name="android.permission.READ_SMS" />
    <!-- Lets the real-time contribution alert (see
         frontend/lib/services/sms_alert_service.dart) notice a new M-PESA
         SMS as it arrives, even while the app is closed, so the user
         doesn't have to remember to open the app and check. Requested via
         the same runtime SMS permission prompt as READ_SMS above (see
         SmsInboxService/permission_handler's Permission.sms group), never
         a second, separate prompt. -->
    <uses-permission android:name="android.permission.RECEIVE_SMS" />
    <application
        android:label="changasmart"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:usesCleartextTraffic="true">
        <!-- Cleartext (plain HTTP) traffic is enabled because this app talks to
             a local development backend (http://<your-computer-ip>:8000) over
             plain HTTP on the same LAN. Fine for local dev; a production build
             pointed at a real HTTPS API would not need this. -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
